<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= filepath; %></title>
    <%- include('partials/meta'); %>
  </head>
  <body>
    <!-- Navigation Bar -->
    <%- include('partials/nav-bar'); %>
    <!-- Checks if any errors exist -->
    <% if (locals.error) { %>
      <%- include('partials/fs-error.ejs'); %>
    <% } %>
  </body>
</html>
